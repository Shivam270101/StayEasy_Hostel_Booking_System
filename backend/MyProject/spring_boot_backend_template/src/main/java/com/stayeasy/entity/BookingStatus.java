package com.stayeasy.entity;



public enum BookingStatus {
    PENDING,    // When booking is requested but not confirmed
    CONFIRMED,  // When booking is confirmed
    CANCELLED   // When user cancels the booking
}

